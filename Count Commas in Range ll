class Solution:
    def countCommas(self, n: int) -> int:
        res = 0
        number = 1000
        while n >= number:
            res += (n - number + 1)
            number *= 1000
        return res
