class Solution:
    def reverseDegree(self, s: str) -> int:
        res = 0
        for i, char in enumerate(s, start = 1):
            rev_alpha = 26 - (ord(char) - ord('a'))
            res += i * rev_alpha
        return res
        
