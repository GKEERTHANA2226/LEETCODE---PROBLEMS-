class Solution:
    def smallestIndex(self, nums: List[int]) -> int:
        for i, num in enumerate(nums):
            sum = 0
            temp = num
            while temp > 0:
                sum += temp % 10
                temp //= 10
            if sum == i:
                return i
        return -1
        
